<!DOCTYPE html>

<title>GaleriePhotos</title>

{% block body %}
    <h1>GaleriePhotos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galerie_photo.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('galerie_photos_index') }}">Revenir à la liste</a>

    <a href="{{ path('galerie_photos_edit', {'id': galerie_photo.id}) }}">Valider</a>

    {{ include('galerie_photos/_delete_form.html.twig') }}
{% endblock %}
